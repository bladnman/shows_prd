Edit documentation using the Document Editor agent.

Read `open-agents/INSTRUCTIONS.md` and then `open-agents/agents/document-editor.md`.

Apply the Document Editor agent to: $ARGUMENTS

If no specific file is mentioned, list available documents in `open-agents/source/` and ask which one to edit.
